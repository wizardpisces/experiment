/**
 * 1. deno run --allow-net --inspect-brk   echo_server.ts
 * 2. Open chrome://inspect and click Inspect next to target:
 * 3. curl http://localhost:8080 然后查看效果
 * 
 * 
 * other example:
 * 
 * deno run --allow-net --unstable --inspect-brk webserver.ts
 */